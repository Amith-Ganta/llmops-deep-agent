apiVersion: v1
kind: Service
metadata:
  name: {{ include "deep-agent.fullname" . }}
  labels:
    {{- include "deep-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deep-agent.selectorLabels" . | nindent 4 }}
